#!/usr/bin/env python3
""" list all """

def list_all(c):
    """ lists all documents """
    if __name__ != "__main__":
        if list(c.find()) and len(list(c.find())):
            return list(c.find())
        else:
            return []
